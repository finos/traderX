# Legacy System Pointer

Canonical system artifacts are maintained under:

- `specs/001-baseline-uncontainerized-parity/system/**`

This `TraderSpec/speckit/system/` directory is pointer-only to avoid broken links during migration.
