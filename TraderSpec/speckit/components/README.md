# Legacy Component Pointer

Canonical component requirement specs are maintained under:

- `specs/001-baseline-uncontainerized-parity/components/*.md`

This `TraderSpec/speckit/components/` directory is pointer-only to avoid broken links during migration.
