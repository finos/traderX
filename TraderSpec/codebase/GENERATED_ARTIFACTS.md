# Generated Artifacts

The following directories are intentionally ephemeral and not source-controlled:

- `generated-components/`
- `generated-manifests/`
- `target-generated/`
- `target-generated-specfirst/` (legacy path, no longer used by canonical runtime)

Recreate them through pipeline commands under `pipeline/`.
