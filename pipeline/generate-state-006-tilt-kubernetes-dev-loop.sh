#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
STATE_ID="006-tilt-kubernetes-dev-loop"

echo "[todo] generation hook not implemented yet for ${STATE_ID}"
echo "[todo] implement generation logic driven by specs/006-tilt-kubernetes-dev-loop"
exit 1
