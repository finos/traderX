apiVersion: v1
kind: Service
metadata:
  name: price-publisher
  namespace: traderx
  labels:
    app: price-publisher
spec:
  type: ClusterIP
  selector:
    app: price-publisher
  ports:
    - name: http
      protocol: TCP
      port: 18100
      targetPort: 18100
