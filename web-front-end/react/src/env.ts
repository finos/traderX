export const Environment = {
	trade_feed_url: `http://${window.location.hostname}/trade-feed`,
	account_service_url:  `http://${window.location.hostname}/account-service`,
	trade_service_url:  `http://${window.location.hostname}:/trade-service`,
	reference_data_url:  `http://${window.location.hostname}/reference-data`,
	people_service_url:  `http://${window.location.hostname}:18095`,
	position_service_url:  `http://${window.location.hostname}/position-service`
}
