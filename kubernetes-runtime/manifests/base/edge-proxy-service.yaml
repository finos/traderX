apiVersion: v1
kind: Service
metadata:
  name: edge-proxy
  namespace: traderx
  labels:
    app: edge-proxy
spec:
  type: NodePort
  selector:
    app: edge-proxy
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
