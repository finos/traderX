apiVersion: v1
kind: Service
metadata:
  name: api-explorer
  namespace: traderx
  labels:
    app: api-explorer
spec:
  selector:
    app: api-explorer
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
      name: http
