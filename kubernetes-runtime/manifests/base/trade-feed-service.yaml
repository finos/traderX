apiVersion: v1
kind: Service
metadata:
  name: trade-feed
  namespace: traderx
  labels:
    app: trade-feed
spec:
  type: ClusterIP
  selector:
    app: trade-feed
  ports:
    - name: http
      protocol: TCP
      port: 18086
      targetPort: 18086
