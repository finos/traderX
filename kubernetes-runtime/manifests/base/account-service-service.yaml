apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: traderx
  labels:
    app: account-service
spec:
  type: ClusterIP
  selector:
    app: account-service
  ports:
    - name: http
      protocol: TCP
      port: 18088
      targetPort: 18088
