/*
 * Database service build configuration for PostgreSQL
 * This build file is now minimal since we use PostgreSQL container
 */

plugins {
    id 'base'
}

// No Java dependencies needed - PostgreSQL runs in container
// This file kept for consistency with Gradle multi-project build