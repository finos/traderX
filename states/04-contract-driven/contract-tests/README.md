# Contract Tests

This directory is reserved for Level 4 contract tests generated from files in `specs/contracts`.

Expected behavior:

- Tests fail on schema drift.
- Tests run independently from Levels 0-3.
