dependencyResolutionManagement {
    repositories {
        // Use Maven Central for resolving dependencies.
        mavenCentral()
    }
}

include 'account-service', ':models','position-service','trade-processor','trade-service','database'
