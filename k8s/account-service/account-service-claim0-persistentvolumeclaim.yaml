apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: account-service-claim0
  name: account-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
