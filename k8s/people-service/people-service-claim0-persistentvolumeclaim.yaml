apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: people-service-claim0
  name: people-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
